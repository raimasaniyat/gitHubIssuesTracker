1️⃣ What is the difference between var, let, and const?
Ans:var is a very old keyword. It has so many glitches. In the modern Javascript people doesn't use it at all. We use let and const. let let's us to call those variables which later can be modified and const doesn't. const means constant.

3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map() method is kind of an array. it's an transformed array which returns a new one. We use it when the logic is small and simple enough to avoid loops.
filter() help to pick the desired array or object. forEach() is quite oppposite than map(). As it doesn;t return anything. It can modify the actual original array.

